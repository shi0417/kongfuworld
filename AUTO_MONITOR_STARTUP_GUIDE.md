# 自动监听程序启动指南

## 🎯 自动监听程序已启动！

### ✅ 当前状态
自动监听程序已经启动，现在会在Chat开发时自动运行检查：

- **开发前**: 自动运行 `npm run auto:start`
- **开发中**: 监听文件变化，自动运行 `npm run auto:during`
- **开发后**: 按 Ctrl+C 退出时自动运行 `npm run auto:post`

## 🚀 启动方式

### 方式1：使用批处理文件（Windows）
```bash
start-auto-monitor.bat
```

### 方式2：使用Shell脚本（Linux/Mac）
```bash
chmod +x start-auto-monitor.sh
./start-auto-monitor.sh
```

### 方式3：直接运行
```bash
node scripts/auto-dev-hooks.js
```

## 📊 自动检查内容

### 开发前检查 (`npm run auto:start`)
- ✅ API端点一致性检查
- ✅ 数据库事务使用检查
- ✅ 数据库连接方式一致性检查 ⚠️ **新增**
- ✅ 错误处理完整性检查

### 开发中检查 (`npm run auto:during`)
- 👀 监听文件变化
- 🔍 自动运行检查
- 📊 生成检查报告

### 开发后检查 (`npm run auto:post`)
- 🏁 全面检查所有功能
- 📋 生成最终报告
- ✅ 确保所有检查通过

## 📝 日志文件

所有检查结果都会保存到日志文件：
- `logs/auto-hooks-{timestamp}.log` - 自动监听程序日志
- `logs/simple-auto-{timestamp}.log` - 检查程序日志

## 🎯 使用说明

### 1. 启动自动监听
```bash
# Windows
start-auto-monitor.bat

# Linux/Mac
./start-auto-monitor.sh
```

### 2. 开始Chat开发
- 自动监听程序会在后台运行
- 文件变化时会自动触发检查
- 按 Ctrl+C 退出时会运行开发后检查

### 3. 查看检查结果
```bash
# 查看最新日志
ls logs/auto-hooks-*.log
ls logs/simple-auto-*.log
```

## 🔧 故障排除

### 1. 自动监听程序不启动
```bash
# 检查Node.js进程
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# 重新启动
node scripts/auto-dev-hooks.js
```

### 2. 检查失败
```bash
# 手动运行检查
npm run auto:start
npm run auto:during
npm run auto:post
```

### 3. 停止自动监听
```bash
# 停止所有Node.js进程
taskkill /F /IM node.exe
```

## 🎉 总结

**自动监听程序已成功启动！**

- ✅ 开发前自动检查
- ✅ 开发中文件变化监听
- ✅ 开发后自动检查
- ✅ 详细日志记录

**重要提醒**：现在每次Chat开发时，自动监听程序会自动运行检查，确保代码质量和一致性！

## 📋 快速命令参考

```bash
# 启动自动监听
start-auto-monitor.bat

# 手动运行检查
npm run auto:start
npm run auto:during
npm run auto:post

# 查看日志
ls logs/auto-hooks-*.log
ls logs/simple-auto-*.log

# 停止监听
taskkill /F /IM node.exe
```
